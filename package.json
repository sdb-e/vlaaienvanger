{
  "name": "vlaaienvanger",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "sync-assets": "node scripts/sync-assets.mjs"
  },
  "dependencies": {
    "phaser": "^3.85.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
